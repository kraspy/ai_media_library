# 🧠 AI Медиа-Библиотека

[Read in English](README.en.md)

**Персональная медиа-библиотека и обучающая платформа, которая превращает ваши медиа-файлы в интерактивные учебные материалы с помощью ИИ.**

Система преобразует видео, аудио и текстовые статьи в структурированные знания, используя большие языковые модели (LLM) для выделения концепций, создания индивидуальных учебных планов и карточек для интервального повторения.

## 🚀 Ключевые Возможности

- **📂 Умная Обработка Медиа**
  - **Мультиформатность:** Поддержка видео, аудио и текста.
  - **Транскрибация:** Автоматическое распознавание речи через **WhisperX** или **OpenAI**.
  - **OCR:** Автоматическое распознавание текста с помощью **Tesseract**.
  - **Анализ:** Умная саммаризация и выделение ключевых тем.

- **🤖 Обучение с ИИ**
  - **Выделение Концепций:** Разбиение сложного контента на небольшие и понятные концепции.
  - **Персональные Учебные Планы:** Генерация тестов (от простого к сложному) на основе загруженных материалов.
  - **Интервальные Повторения:** Автоматическое создание карточек для долгосрочного запоминания (SRS).

- **👩‍🏫 AI Учитель**
  - **Умный Чат:** Общение с AI-учителем, который имеет доступ ко всей вашей библиотеке знаний.
  - **Подробный разбор тем:** Возможность задать вопросы по учебным планам или отдельным концепциям.

- **🧠 Система SRS**
  - Встроенная система Карточек для долгосрочного запоминания.
  - Умное планирование времени будущих повторений на основе индивидуального уровня запоминания.

## 🛠️ Технологический Стек

- **Backend:** Python 3.13, Django 5.2
- **Async Tasks:** Redis + Celery
- **AI/ML:** 
    - **LLM:** OpenAI, DeepSeek, LangChain
    - **Speech-to-Text:** WhisperX
    - **Vector Store:** ChromaDB
    - **OCR:** Tesseract
- **Database:** PostgreSQL
- **Infrastructure:** Docker, uv (package manager)

## ⚡ Быстрый Старт

### Требования
- Python 3.13+
- [uv](https://github.com/astral-sh/uv)
- Docker и Docker Compose (для PostgreSQL и Redis)
- **Системные утилиты**:
  - `ffmpeg` (для обработки аудио)
  - `tesseract` + языковые пакеты `eng`, `rus` (для OCR)

### Установка и Запуск

1.  **Клонирование репозитория**
    ```bash
    git clone https://github.com/kraspy/ai_media_library.git
    cd ai_media_library
    ```

2.  **Запуск инфраструктуры (БД и Redis)**
    Используйте Docker Compose для быстрого запуска:
    ```bash
    docker compose up -d
    ```

3.  **Настройка окружения**
    Создайте файл `.env` на основе `.env.example`:
    ```bash
    cp .env.example .env
    ```
    Откройте `.env` и укажите ваши API ключи:
    ```env
    # Database (по умолчанию настроено для docker compose)
    DATABASE_URL=postgres://postgres:password@127.0.0.1:5432/aml_db

    # LLM Keys
    OPENAI_API_KEY=sk-...
    OPENAI_MODEL=gpt-...
    DEEPSEEK_API_KEY=sk-...
    ```

4.  **Установка зависимостей**
    Используйте `uv` для синхронизации окружения:
    ```bash
    uv sync
    ```

5.  **Применение миграций**
    ```bash
    uv run python manage.py migrate
    ```

6.  **Запуск компонентов**

    **Терминал 1: Веб-сервер Django**
    ```bash
    uv run python manage.py runserver
    ```

    **Терминал 2: Celery Worker**
    ```bash
    # Для Linux/MacOS
    uv run celery -A config worker -l info
    ```

    Приложение доступно по адресу: http://127.0.0.1:8000

## 📚 Структура Проекта

- `apps/` - Основные модули Django.
- `docker-build/` - Dockerfile для сборки.
- `config/` - Настройки проекта.